package enums;

// Every enum internally extends an abstract class called Enum
public enum EnumsExample{

    // all these values of objects of the EnumsExample class, and it's type will be public static final.
    ENUM1, ENUM2, ENUM3
}
